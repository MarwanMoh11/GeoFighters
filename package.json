{
  "name": "geofighters",
  "version": "1.0.0",
  "description": "A Three.js geometry-based survival game",
  "main": "js/main.js",
  "scripts": {
    "start": "npx serve .",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "private": true,
  "dependencies": {
    "three": "^0.180.0"
  }
}